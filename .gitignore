# PowerShell transcripts / install logs
*.log
PSInstallLog.txt

# Local secrets — never commit API keys, tokens, or credential files
secrets.ps1
*.secrets.ps1
*.credential
*.pfx
*.key
*.api-key
*.partner-key
.ironscales-*-key
.*-api-key
.*-partner-key

# Claude Code per-user/local settings (shared settings.json is OK)
.claude/settings.local.json
.claude/*.local.json

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Windows / OneDrive sync artifacts
*:Zone.Identifier
~$*
